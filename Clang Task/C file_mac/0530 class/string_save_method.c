#include <stdio.h>
#include <string.h>
#define _CRT_SECURE_NO_WARNINGS
int main(){
    char str[] = "elephant";
    puts(str);
    // char str[80];
    // char str2[100];
    // gets(str);
    //  strcpy(str2,str);
    // puts(str);
    // puts(str2);

    return 0;
}